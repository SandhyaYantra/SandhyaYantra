# CleanRecipe Engine

A standalone browser tool for scaling recipes, preparing ingredients, and running kitchen stages.

Open `index.html` directly or serve this folder with any static web server. The app has no build step and stores recipes, menus, theme, and progress in the browser's local storage.
